import { test, expect } from "@playwright/test";

test.describe("Performance limits", () => {
  test("1000-node DOM scanned in under 2 seconds", async () => {
    /* TODO */
  });
  test("async detector does not block input events during scan", async () => {
    /* TODO */
  });
  test("repeated scans do not degrade (no memory accumulation)", async () => {
    /* TODO */
  });
  test("5-URL sitemap scan completes in under 30 seconds", async () => {
    /* TODO */
  });
});
