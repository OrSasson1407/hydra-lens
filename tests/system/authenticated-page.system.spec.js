import { test } from "@playwright/test";
test.describe("Authenticated pages", () => {
    test("unauthenticated fetch returns login redirect HTML", async () => {
        /* TODO */
    });
    test("redirect HTML vs real DOM → not flagged as hydration mismatch", async () => {
        /* TODO */
    });
    test("error message explains auth limitation to user", async () => {
        /* TODO */
    });
});
//# sourceMappingURL=authenticated-page.system.spec.js.map