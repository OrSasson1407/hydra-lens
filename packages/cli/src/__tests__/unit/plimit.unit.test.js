import { describe, it } from "vitest";
describe("pLimit", () => {
    it("pLimit(tasks, 1) runs all tasks in series", () => {
        /* TODO */
    });
    it("pLimit(tasks, N) runs at most N tasks concurrently", () => {
        /* TODO */
    });
    it("results are returned in original task order", () => {
        /* TODO */
    });
    it("rejected task propagates the error", () => {
        /* TODO */
    });
    it("limit larger than task count → all run in parallel", () => {
        /* TODO */
    });
});
//# sourceMappingURL=plimit.unit.test.js.map