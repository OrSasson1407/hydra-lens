import { describe, it, expect } from "vitest";

describe("scan-page.integration", () => {
  it("scanPage returns mismatches array for known fixture", () => {
    /* TODO */
  });
  it("scanPage returns error field on navigation failure", () => {
    /* TODO */
  });
  it("scanPage closes the page (not the browser) after scan", () => {
    /* TODO */
  });
  it("durationMs is a positive number", () => {
    /* TODO */
  });
  it("securityOnly=true → only security mismatches returned", () => {
    /* TODO */
  });
  it("shared browser instance is reused across calls", () => {
    /* TODO */
  });
});
