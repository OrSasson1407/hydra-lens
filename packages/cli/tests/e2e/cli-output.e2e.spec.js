import { test } from "@playwright/test";
test.describe("CLI Output arguments", () => {
    test("--output report.json → file is created", async () => {
        /* TODO */
    });
    test("report.json is valid JSON array", async () => {
        /* TODO */
    });
    test("each entry has url, mismatches, durationMs fields", async () => {
        /* TODO */
    });
    test("mismatches contain selector, severity, advice fields", async () => {
        /* TODO */
    });
    test("report omitted when --output not passed", async () => {
        /* TODO */
    });
});
//# sourceMappingURL=cli-output.e2e.spec.js.map