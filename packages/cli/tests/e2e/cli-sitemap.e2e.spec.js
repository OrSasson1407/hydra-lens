import { test } from "@playwright/test";
test.describe("CLI Sitemap scanning", () => {
    test("--sitemap scans all URLs in the sitemap XML", async () => {
        /* TODO */
    });
    test("each URL appears as a row in SCAN SUMMARY", async () => {
        /* TODO */
    });
    test("duplicate URLs (argv + sitemap) scanned only once", async () => {
        /* TODO */
    });
    test("one failing URL does not abort remaining scans", async () => {
        /* TODO */
    });
});
//# sourceMappingURL=cli-sitemap.e2e.spec.js.map