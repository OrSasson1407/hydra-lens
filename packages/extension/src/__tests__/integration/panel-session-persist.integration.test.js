import { describe, it } from "vitest";
describe("panel-session-persist.integration", () => {
    it("HYDRALENS_RESULTS message → saved to storage.session", () => {
        /* TODO */
    });
    it("panel init with stored results → rendered immediately", () => {
        /* TODO */
    });
    it('panel init with empty storage → "Waiting for scan" shown', () => {
        /* TODO */
    });
    it("new scan overwrites old session data", () => {
        /* TODO */
    });
    it("ignore-selector button updates storage.local ignoredSelectors", () => {
        /* TODO */
    });
});
