import { test, expect } from "@playwright/test";

test.describe("Extension DevTools Panel", () => {
  test("DevTools panel shows results after scan", async () => {
    /* TODO */
  });
  test("panel shows previous results when opened after scan", async () => {
    /* TODO */
  });
  test("severity filter hides non-matching cards", async () => {
    /* TODO */
  });
  test('"Ignore Selector" button removes overlay from page', async () => {
    /* TODO */
  });
  test('"Clear Ignore List" re-shows previously ignored overlays', async () => {
    /* TODO */
  });
  test('"Rescan Page" button triggers a fresh scan', async () => {
    /* TODO */
  });
});
